import React from 'react';

/**
 * Replaced by the Header (Navbar) direct messages & notices popover.
 * The floating widget has been completely removed per user request.
 */
export const FloatingChatButton: React.FC<any> = () => {
  return null;
};

